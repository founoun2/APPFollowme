rules_version='2'

service cloud.firestore {
  match /databases/{database}/documents {
    match /{document=**} {
      // Allow read/write on all documents to authenticated users only
      allow read, write: if request.auth != null;
    }
  }
}
